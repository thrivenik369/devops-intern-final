1. Run Loki:
   docker run -d --name=loki -p 3100:3100 grafana/loki:2.9.2
2. Forward logs (example):
   docker logs <container_name> 2>&1 | curl -XPOST http://localhost:3100/loki/api/v1/push
3. Query logs:
   open Grafana UI at http://localhost:3000
   Add Loki as a data source and query logs.
